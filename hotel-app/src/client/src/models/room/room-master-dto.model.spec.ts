import { RoomMasterDtoModel } from './room-master-dto.model';

describe('RoomMasterDtoModel', () => {
  it('should create an instance', () => {
    expect(new RoomMasterDtoModel()).toBeTruthy();
  });
});
